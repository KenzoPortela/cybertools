[
  {
    "name": "cyberchef-to-hex",
    "url": "http://localhost:5173/tools/to-hex",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"abc\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"61 62 63\") || ('attendu ' + \"61 62 63\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-from-hex",
    "url": "http://localhost:5173/tools/from-hex",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"61 62 63\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"abc\") || ('attendu ' + \"abc\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-rot13",
    "url": "http://localhost:5173/tools/rot13",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"Hello\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"Uryyb\") || ('attendu ' + \"Uryyb\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-to-base32",
    "url": "http://localhost:5173/tools/to-base32",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"abc\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"MFRGG===\") || ('attendu ' + \"MFRGG===\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-md4",
    "url": "http://localhost:5173/tools/md4",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"abc\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"a448017aaf21d8525fc10ae87aa6729d\") || ('attendu ' + \"a448017aaf21d8525fc10ae87aa6729d\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-whirlpool",
    "url": "http://localhost:5173/tools/whirlpool",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"abc\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"4e2448a4c6f486bb16b6562c73b4020bf3043e3a731bce721ae1b303d97e6d4c7181eebdb6c57e277d0e34957114cbd6c797fc9d95d8b582d225292076d4eef5\") || ('attendu ' + \"4e2448a4c6f486bb16b6562c73b4020bf3043e3a731bce721ae1b303d97e6d4c7181eebdb6c57e277d0e34957114cbd6c797fc9d95d8b582d225292076d4eef5\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-vigenere-encode",
    "url": "http://localhost:5173/tools/vigenere-encode",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"hello\");\n    for (const [index, value] of Object.entries({\"0\":\"key\"})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"rijvs\") || ('attendu ' + \"rijvs\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-gzip",
    "url": "http://localhost:5173/tools/gzip",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"hello hello hello\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    if (!outcome.binary) return 'sortie binaire attendue, vu : ' + outcome.text.slice(0, 80);\n    return true;\n  })()"
  },
  {
    "name": "cyberchef-bzip2-compress",
    "url": "http://localhost:5173/tools/bzip2-compress",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"hello hello hello\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"BZh\") || ('attendu ' + \"BZh\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-argon2",
    "url": "http://localhost:5173/tools/argon2",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"abc\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"$argon2\") || ('attendu ' + \"$argon2\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-jq",
    "url": "http://localhost:5173/tools/jq",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"{\\\"a\\\":42}\");\n    for (const [index, value] of Object.entries({\"0\":\".a\"})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"42\") || ('attendu ' + \"42\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-yara-rules",
    "url": "http://localhost:5173/tools/yara-rules",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"hello world\");\n    for (const [index, value] of Object.entries({\"0\":\"rule salut { strings: $a = \\\"hello\\\" condition: $a }\"})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"salut\") || ('attendu ' + \"salut\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-disassemble-arm",
    "url": "http://localhost:5173/tools/disassemble-arm",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"00 00 a0 e1\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"mov\") || ('attendu ' + \"mov\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-disassemble-x86",
    "url": "http://localhost:5173/tools/disassemble-x86",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"90\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return outcome.text?.includes(\"NOP\") || ('attendu ' + \"NOP\" + ', vu : ' + (outcome.text ?? outcome.html ?? '').slice(0, 160));\n  })()"
  },
  {
    "name": "cyberchef-entropy",
    "url": "http://localhost:5173/tools/entropy",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    \n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"hello world\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    return !!outcome.html || 'sortie HTML attendue';\n    \n    return true;\n  })()"
  },
  {
    "name": "cyberchef-datetime-delta",
    "url": "http://localhost:5173/tools/datetime-delta",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => {\n    const sleep = ms => new Promise(r => setTimeout(r, ms));\n    const until = async (test, timeout) => {\n      for (const started = performance.now(); performance.now() - started < timeout;) {\n        const value = test();\n        if (value) return value;\n        await sleep(100);\n      }\n      return null;\n    };\n    const set = (element, value) => {\n      element.value = value;\n      element.dispatchEvent(new Event('input', { bubbles: true }));\n    };\n\n    if (!await until(() => document.querySelector('.cc-op .io'), 15000)) return 'formulaire absent';\n    return ((() => { const value = [...document.querySelectorAll('.cc-op .params-grid > .field')][1]?.querySelector('input')?.value; return !!value || 'format d\\'entrée vide'; })());\n\n    const fields = [...document.querySelectorAll('.cc-op .params-grid > .field')];\n    set(document.querySelector('.cc-op .io textarea'), \"\");\n    for (const [index, value] of Object.entries({})) {\n      const control = fields[index]?.querySelector('textarea, input');\n      if (!control) return 'argument ' + index + ' introuvable';\n      set(control, value);\n    }\n    await sleep(400);\n\n    // Premier état stable, une fois le calcul terminé : une erreur, du HTML, ou\n    // une sortie texte non vide. Sans attendre la fin du calcul, on risquerait de\n    // lire le résultat d'une frappe précédente.\n    const outcome = await until(() => {\n      if (document.querySelector('.cc-op [aria-busy=\"true\"]')) return null;\n      const error = document.querySelector('.cc-op .op-error');\n      if (error) return { error: error.innerText.trim().slice(0, 200) };\n      const html = document.querySelector('.cc-op .html-output');\n      if (html) return { html: html.innerHTML };\n      const out = document.querySelectorAll('.cc-op .io textarea')[1]?.value;\n      if (out) return { text: out, binary: !!document.querySelector('.cc-op .binary-notice') };\n      return null;\n    }, 30000);\n\n    if (!outcome) return 'aucune sortie en 30 s';\n    if (outcome.error) return 'erreur : ' + outcome.error;\n    \n    \n    return true;\n  })()"
  }
]
