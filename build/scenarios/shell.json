[
  {
    "name": "accueil-clair",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(() => { const rail = document.querySelector('.rail'); const cats = rail.querySelectorAll('a[href^=\"/categories/\"]'); const counts = [...rail.querySelectorAll('a[href^=\"/categories/\"] .rail-count')].map(c => Number(c.textContent)); const crumb = document.querySelector('.top-bar .crumb')?.textContent.replace(/\\s+/g, ''); const ok = cats.length === 10 && counts.every(n => n > 0) && Math.round(rail.getBoundingClientRect().width) === 224 && !rail.inert && crumb === '~/home' && getComputedStyle(document.querySelector('.top-bar-menu')).display === 'none'; return ok || { cats: cats.length, counts, width: rail.getBoundingClientRect().width, crumb }; })()"
  },
  {
    "name": "accueil-sombre-large",
    "url": "http://localhost:5173/",
    "width": 1440,
    "height": 900,
    "scheme": "dark",
    "script": "(() => { const page = document.querySelector('.page').getBoundingClientRect(); const main = document.querySelector('.shell-content').getBoundingClientRect(); return (page.width <= 1120 && Math.abs((page.left - main.left) - (main.right - page.right)) < 2) || { page: page.width, main: main.width }; })()"
  },
  {
    "name": "accueil-mobile",
    "url": "http://localhost:5173/",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "light",
    "script": "(() => { const tabs = [...document.querySelectorAll('.tab-bar .tab')]; const sizes = tabs.map(t => Math.round(t.getBoundingClientRect().height)); const rail = document.querySelector('.rail'); const hidden = rail.inert && rail.getBoundingClientRect().right <= 0; const status = getComputedStyle(document.querySelector('.status-bar')).display; return (tabs.length === 4 && sizes.every(h => h >= 44) && hidden && status === 'none') || { tabs: tabs.length, sizes, hidden, status }; })()"
  },
  {
    "name": "tiroir-ouvert",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 900,
    "height": 800,
    "scheme": "dark",
    "script": "(async () => { const menu = document.querySelector('.top-bar-menu'); menu.click(); await new Promise(r => setTimeout(r, 400)); const rail = document.querySelector('.rail'); const r = rail.getBoundingClientRect(); return (!rail.inert && r.left === 0 && document.querySelector('.shell-main').inert === true && menu.getAttribute('aria-expanded') === 'true' && rail.contains(document.activeElement)) || { left: r.left, inert: document.querySelector('.shell-main').inert, focus: document.activeElement?.className }; })()"
  },
  {
    "name": "tiroir-echap",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 900,
    "height": 800,
    "scheme": "light",
    "script": "(async () => { const menu = document.querySelector('.top-bar-menu'); menu.click(); await new Promise(r => setTimeout(r, 400)); document.activeElement.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true })); await new Promise(r => setTimeout(r, 400)); const rail = document.querySelector('.rail'); return (rail.inert && !document.querySelector('.shell-main').inert && document.activeElement === menu) || { inert: rail.inert, focus: document.activeElement?.className }; })()"
  },
  {
    "name": "tiroir-lien",
    "url": "http://localhost:5173/",
    "width": 900,
    "height": 800,
    "scheme": "light",
    "script": "(async () => { document.querySelector('.top-bar-menu').click(); await new Promise(r => setTimeout(r, 400)); document.querySelector('.rail a[href=\"/categories/crypto\"]').click(); await new Promise(r => setTimeout(r, 800)); return (location.pathname === '/categories/crypto' && document.querySelector('.rail').inert) || location.pathname; })()"
  },
  {
    "name": "mobile-epingles",
    "url": "http://localhost:5173/",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => { [...document.querySelectorAll('.tab-bar .tab')][3].click(); await new Promise(r => setTimeout(r, 400)); return (document.activeElement?.id === 'rail-pinned' && !document.querySelector('.rail').inert) || document.activeElement?.id; })()"
  },
  {
    "name": "a-propos",
    "url": "http://localhost:5173/about",
    "scheme": "dark",
    "script": "(() => { const links = [...document.querySelectorAll('.author a, .status-bar a')].map(a => a.getAttribute('href')); return (links.includes('https://github.com/KenzoPortela') && links.includes('https://kenzoportela.com') && /Kenzo Portela/.test(document.querySelector('.status-bar').textContent)) || links; })()"
  },
  {
    "name": "404",
    "url": "http://localhost:5173/page-inexistante",
    "scheme": "light",
    "script": "document.querySelector('h1')?.textContent.trim() === '404' && document.querySelector('.top-bar .crumb')?.textContent.replace(/\\s+/g, '') === '~/404'"
  },
  {
    "name": "outil-base64",
    "url": "http://localhost:5173/tools/base64-string-converter",
    "scheme": "light",
    "script": "(async () => { const [src, out] = document.querySelectorAll('textarea'); src.value = 'Bonjour CyberTools'; src.dispatchEvent(new Event('input', { bubbles: true })); await new Promise(r => setTimeout(r, 400)); const crumb = document.querySelector('.top-bar .crumb')?.textContent.replace(/\\s+/g, ''); const active = document.querySelector('.rail .rail-item--active')?.getAttribute('href'); return (out.value === 'Qm9uam91ciBDeWJlclRvb2xz' && crumb === '~/encoding/base64-string-converter' && active === '/categories/encoding') || { out: out.value, crumb, active }; })()"
  },
  {
    "name": "outil-hash-mobile",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(() => { const button = document.querySelector('#ct-topbar-actions .recipes-link'); return (button?.getAttribute('aria-label') === 'Open in Recipes' && getComputedStyle(button.querySelector('.recipes-link-label')).display === 'none') || button?.outerHTML.slice(0, 200); })()"
  },
  {
    "name": "atelier-pleine-largeur",
    "url": "http://localhost:5173/tools/recipes",
    "width": 1440,
    "height": 900,
    "scheme": "dark",
    "script": "(() => { const tool = document.querySelector('.tool'); const active = document.querySelector('.rail .rail-item--active')?.getAttribute('href'); return (tool.classList.contains('tool--workspace') && tool.getBoundingClientRect().width > 1120 && active === '/tools/recipes' && document.querySelectorAll('.rail .rail-item--active').length === 1) || { width: tool.getBoundingClientRect().width, active }; })()"
  },
  {
    "name": "cycle-theme",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(async () => { localStorage.removeItem('cybertools:theme'); const btn = () => [...document.querySelectorAll('button')].find(b => /Thème|Theme/.test(b.getAttribute('aria-label') || '')); const seen = []; for (let i = 0; i < 3; i++) { btn().click(); await new Promise(r => setTimeout(r, 250)); seen.push(localStorage.getItem('cybertools:theme') + ':' + document.documentElement.classList.contains('dark')); } return seen.join(' → ') === 'light:false → dark:true → auto:false'; })()"
  },
  {
    "name": "bascule-langue",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(async () => { const btn = document.querySelector('.rail-locale'); const before = btn.textContent.trim(); btn.click(); await new Promise(r => setTimeout(r, 300)); const after = btn.textContent.trim(); const label = document.querySelector('#rail-workbench').textContent.trim(); btn.click(); await new Promise(r => setTimeout(r, 300)); return (before === 'EN' && after === 'FR' && label === 'Atelier') || { before, after, label }; })()"
  },
  {
    "name": "lien-parametres",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(async () => { document.querySelector('.settings-button').click(); await new Promise(r => setTimeout(r, 800)); return location.pathname === '/settings' && document.querySelector('h1')?.textContent.trim() === 'Settings'; })()"
  },
  {
    "name": "design-system",
    "url": "http://localhost:5173/_design/c-button",
    "scheme": "dark",
    "fullPage": true
  },
  {
    "name": "etat-accueil",
    "url": "http://localhost:5173/",
    "scheme": "dark",
    "script": "(() => { const left = document.querySelector('.status-left').textContent.replace(/\\s+/g, ' ').trim(); const net = document.querySelector('.status-network'); return (/^\\d+ tools indexed$/.test(left) && net.textContent.trim() === '0 external requests' && !!net.querySelector('.status-dot--live')) || { left, net: net.textContent.trim() }; })()"
  },
  {
    "name": "etat-operation",
    "url": "http://localhost:5173/tools/to-hex",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => { const sleep = ms => new Promise(r => setTimeout(r, ms)); const until = async (test, timeout) => { for (const started = performance.now(); performance.now() - started < timeout;) { const value = test(); if (value) return value; await sleep(100); } return null; }; const items = () => [...document.querySelectorAll('.status-left .status-item')].map(e => e.textContent.replace(/\\s+/g, ' ').trim()); if (!await until(() => document.querySelector('.cc-op .io textarea'), 15000)) return 'formulaire absent'; const src = document.querySelector('.cc-op .io textarea'); src.value = 'hello'; src.dispatchEvent(new Event('input', { bubbles: true })); const seen = await until(() => (items().length === 2 && items()[1].startsWith('5 o') ? items() : null), 20000); return (seen && seen[0] === 'source: CyberChef' && /^5 o → 14 o · [\\d.,]+ ms · 1 operation$/.test(seen[1]) && !!document.querySelector('.status-left .status-dot--success')) || items(); })()"
  },
  {
    "name": "etat-session",
    "url": "http://localhost:5173/tools/to-hex",
    "scheme": "light",
    "wait": 200,
    "script": "(async () => { const sleep = ms => new Promise(r => setTimeout(r, ms)); const until = async (test, timeout) => { for (const started = performance.now(); performance.now() - started < timeout;) { const value = test(); if (value) return value; await sleep(100); } return null; }; const items = () => [...document.querySelectorAll('.status-left .status-item')].map(e => e.textContent.replace(/\\s+/g, ' ').trim()); if (!await until(() => document.querySelector('.cc-op .io textarea'), 15000)) return 'formulaire absent'; const src = document.querySelector('.cc-op .io textarea'); src.value = 'hello'; src.dispatchEvent(new Event('input', { bubbles: true })); const seen = await until(() => (items().length === 2 && items()[1].startsWith('5 o') ? items() : null), 20000); if (!seen) return 'pas de calcul'; document.querySelector('.rail-footer a[href=\"/about\"]').click(); await sleep(800); const left = document.querySelector('.status-left').textContent.replace(/\\s+/g, ' ').trim(); return (location.pathname === '/about' && /^session: [1-9]\\d* operations? · \\d+ o processed$/.test(left)) || left; })()"
  },
  {
    "name": "etat-reseau",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(async () => { const img = new Image(); img.src = 'http://[::1]:5173/favicon.svg?sonde=' + Date.now(); await new Promise(r => { img.onload = img.onerror = r; }); await new Promise(r => setTimeout(r, 500)); const net = document.querySelector('.status-network'); return (net.textContent.trim() === '1 external request' && !!net.querySelector('.status-dot--warning') && net.title.includes('http://[::1]:5173')) || { text: net.textContent.trim(), title: net.title }; })()"
  },
  {
    "name": "outil-en-tete",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 1440,
    "height": 900,
    "scheme": "dark",
    "script": "(() => { const title = document.querySelector('.tool .title'); const rail = document.querySelector('.tool-context-rail'); const links = [...rail.querySelectorAll('.context-link')].map(a => a.getAttribute('href')); const more = rail.querySelector('.context-more')?.getAttribute('href'); const ok = !!document.querySelector('#ct-topbar-actions .favorite') && getComputedStyle(title).fontSize === '20px' && getComputedStyle(rail).display === 'flex' && links.length === 8 && !links.includes('/tools/hash-text') && more === '/categories/crypto'; return ok || { favori: !!document.querySelector('#ct-topbar-actions .favorite'), taille: getComputedStyle(title).fontSize, rail: getComputedStyle(rail).display, links, more }; })()"
  },
  {
    "name": "outil-contexte-masque",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 1200,
    "height": 800,
    "scheme": "light",
    "script": "getComputedStyle(document.querySelector('.tool-context-rail')).display === 'none'"
  },
  {
    "name": "rail-epingle-prepa",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(() => { localStorage.setItem('cybertools:favorites', JSON.stringify(['hash-text', 'to-hex'])); return true; })()"
  },
  {
    "name": "rail-epingle",
    "url": "http://localhost:5173/",
    "scheme": "light",
    "script": "(() => { const sizes = [...document.querySelectorAll('#ct-rail-pinned .rail-icon')].map(i => Math.round(i.getBoundingClientRect().width) + 'x' + Math.round(i.getBoundingClientRect().height)); localStorage.removeItem('cybertools:favorites'); return (sizes.length === 2 && sizes.every(s => s === '16x16')) || sizes; })()"
  },
  {
    "name": "rail-replie",
    "url": "http://localhost:5173/tools/hash-text",
    "scheme": "dark",
    "script": "(async () => { document.querySelector('.rail-collapse').click(); await new Promise(r => setTimeout(r, 500)); const rail = document.querySelector('.rail'); const label = rail.querySelector('a[href=\"/categories/crypto\"] .rail-item-label'); const settings = JSON.parse(localStorage.getItem('cybertools:settings') || '{}'); const stored = settings.shellRailCollapsed; localStorage.setItem('cybertools:settings', JSON.stringify({ ...settings, shellRailCollapsed: false })); return (Math.round(rail.getBoundingClientRect().width) === 48 && label.textContent.trim().length > 0 && label.getBoundingClientRect().width <= 1 && stored === true) || { width: rail.getBoundingClientRect().width, stored }; })()"
  }
]
