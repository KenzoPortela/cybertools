[
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-remise-a-zero",
    "url": "http://localhost:5173/",
    "script": "(() => { localStorage.removeItem('cybertools:recipe'); return true; })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-vide",
    "url": "http://localhost:5173/tools/recipes",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await until(() => document.querySelector('.workbench .empty'));\n      // Sans recette précédente, pas de bouton « Reprendre ».\n      return (!!document.querySelector('.workbench .empty') && !document.querySelector('.resume')) || 'état vide incorrect';\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-composer",
    "url": "http://localhost:5173/tools/recipes",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await addOperation('from base64');\n      await addOperation('to hex');\n      type(inputField(), 'aGVsbG8=');\n      const out = await output();\n      await sleep(900);\n      const names = stepNames();\n      const ok = names.join(' + ') === 'From Base64 + To Hex' && out === '68 65 6c 6c 6f' && location.hash.startsWith('#recipe=From_Base64');\n      return ok || { étapes: names, sortie: out, adresse: location.hash.slice(0, 80) };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-lien-cyberchef",
    "url": "http://localhost:5173/tools/recipes#recipe=From_Base64('A-Za-z0-9%2B/%3D',true,false)To_Hex('Space',0)&input=YUdWc2JHOD0",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await until(() => stepNames().length === 2);\n      const out = await output();\n      await sleep(900);\n      // L'alphabet contient un « + » : il doit survivre à la lecture du lien, puis\n      // à la réécriture de l'adresse (encodé une seule fois, en %2B).\n      const alphabet = document.querySelector('.steps .step .params-grid .field input')?.value;\n      const ok = stepNames().join(' + ') === 'From Base64 + To Hex' && inputField().value === 'aGVsbG8=' && out === '68 65 6c 6c 6f'\n        && alphabet === 'A-Za-z0-9+/=' && location.hash.includes(\"A-Za-z0-9%2B/%3D\") && !location.hash.includes('%25');\n      return ok || { étapes: stepNames(), entrée: inputField()?.value, sortie: out, alphabet, adresse: location.hash.slice(0, 90) };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-recherche-souple",
    "url": "http://localhost:5173/tools/recipes",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      const field = document.querySelector('.picker .field-input');\n      field.focus();\n      type(field, 'caeser cipher');\n      await sleep(300);\n      const options = [...document.querySelectorAll('.picker [role=option]')].map(e => e.textContent.trim());\n      field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true }));\n      type(field, '');\n      const found = name => options.some(text => text.startsWith(name));\n      return (found('Caesar Box Cipher') && found('ROT13')) || { options: options.slice(0, 6) };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-reprise",
    "url": "http://localhost:5173/tools/recipes",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      const resume = await until(() => document.querySelector('.workbench .empty .resume'));\n      if (!resume) return { vide: !!document.querySelector('.workbench .empty'), étapes: stepNames() };\n      resume.click();\n      await until(() => stepNames().length);\n      return stepNames().join(' + ') === 'From Base64 + To Hex' || { étapes: stepNames() };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-etape-en-echec",
    "url": "http://localhost:5173/tools/recipes#recipe=From_Hex('Auto')Jq('.a%5B',false)&input=N2I3ZA%3D%3D",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await until(() => stepNames().length === 2);\n      await output();\n      await sleep(400);\n      const failed = [...document.querySelectorAll('.steps .step')].findIndex(e => e.classList.contains('step--failed'));\n      const warning = document.querySelector('.stopped-notice, .op-error')?.innerText.trim();\n      return (failed === 1 && /2/.test(warning ?? '')) || { étapeEnÉchec: failed, message: warning };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-operation-ecartee",
    "url": "http://localhost:5173/tools/recipes#recipe=To_Upper_case('All')HTTP_request('GET','https://example.com','',false,false)",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await until(() => document.querySelector('.workbench .notice'));\n      const notice = document.querySelector('.workbench .notice')?.innerText ?? '';\n      return (stepNames().join() === 'To Upper case' && notice.includes('HTTP request')) || { étapes: stepNames(), avis: notice };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-magic",
    "url": "http://localhost:5173/tools/recipes",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      [...document.querySelectorAll('.toolbar-actions button')].find(b => /Vider|Clear/.test(b.textContent)).click();\n      await sleep(300);\n      type(inputField(), 'aGVsbG8gd29ybGQ=');\n      [...document.querySelectorAll('.toolbar-actions button')].find(b => b.textContent.includes('Magic')).click();\n      const first = await until(() => document.querySelector('.magic-card .candidate'), 20000);\n      if (!first) return 'aucun candidat Magic';\n      const label = first.querySelector('.candidate-recipe').textContent;\n      first.querySelector('button').click();\n      const out = await output();\n      return (label.includes('From Base64') && stepNames().includes('From Base64') && out === 'hello world') || { candidat: label, étapes: stepNames(), sortie: out };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "magic-outil",
    "url": "http://localhost:5173/tools/magic",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      type(document.querySelector('.magic-tool textarea'), '68656c6c6f20776f726c64');\n      const candidate = await until(() => [...document.querySelectorAll('.magic-tool .candidate')].find(c => c.querySelector('.candidate-recipe').textContent.includes('From Hex')), 20000);\n      if (!candidate) return 'pas de piste From Hex';\n      candidate.querySelector('button').click();\n      await until(() => location.pathname === '/tools/recipes' && stepNames().length);\n      const out = await output();\n      return (stepNames().includes('From Hex') && out === 'hello world') || { adresse: location.pathname, étapes: stepNames(), sortie: out };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "magic-auto-1",
    "url": "http://localhost:5173/tools/magic",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      type(document.querySelector('.magic-tool textarea'), 'aGVsbG8gd29ybGQ=');\n      const status = await until(() => /(détectée|Detected).*1/.test(document.querySelector('.depth-status')?.textContent ?? '') && document.querySelector('.depth-status'), 30000);\n      const first = document.querySelector('.magic-tool .candidate');\n      const label = first?.querySelector('.candidate-recipe').textContent.trim();\n      const data = first?.querySelector('.candidate-data').textContent.trim();\n      return (!!status && label === 'From Base64' && data === 'hello world') || { statut: document.querySelector('.depth-status')?.textContent.trim(), piste: label, donnée: data };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "magic-auto-2",
    "url": "http://localhost:5173/tools/magic",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      type(document.querySelector('.magic-tool textarea'), 'Njg2NTZjNmM2ZjIwNzc2ZjcyNmM2NA==');\n      const status = await until(() => /(détectée|Detected).*2/.test(document.querySelector('.depth-status')?.textContent ?? '') && document.querySelector('.depth-status'), 30000);\n      const first = document.querySelector('.magic-tool .candidate');\n      const label = first?.querySelector('.candidate-recipe').textContent.trim();\n      const data = first?.querySelector('.candidate-data').textContent.trim();\n      return (!!status && label === 'From Base64 → From Hex' && data === 'hello world') || { statut: document.querySelector('.depth-status')?.textContent.trim(), piste: label, donnée: data };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "magic-auto-3",
    "url": "http://localhost:5173/tools/magic",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      type(document.querySelector('.magic-tool textarea'), 'TmpnMk5UWmpObU0yWmpJd056YzJaamN5Tm1NMk5BPT0=');\n      const status = await until(() => /(détectée|Detected).*3/.test(document.querySelector('.depth-status')?.textContent ?? '') && document.querySelector('.depth-status'), 30000);\n      const first = document.querySelector('.magic-tool .candidate');\n      const label = first?.querySelector('.candidate-recipe').textContent.trim();\n      const data = first?.querySelector('.candidate-data').textContent.trim();\n      return (!!status && label === 'From Base64 → From Base64 → From Hex' && data === 'hello world') || { statut: document.querySelector('.depth-status')?.textContent.trim(), piste: label, donnée: data };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "magic-auto-clair",
    "url": "http://localhost:5173/tools/magic",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      type(document.querySelector('.magic-tool textarea'), 'Just some plain English text here.');\n      const status = await until(() => /(Rien à décoder|Nothing to decode)/.test(document.querySelector('.depth-status')?.textContent ?? ''), 30000);\n      return !!status || document.querySelector('.depth-status')?.textContent.trim();\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "magic-manuel",
    "url": "http://localhost:5173/tools/magic",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      type(document.querySelector('.magic-tool textarea'), 'aGVsbG8gd29ybGQ=');\n      [...document.querySelectorAll('.depth-mode label')][1].click();\n      await sleep(400);\n      const input = document.querySelector('.magic .depth input');\n      const candidate = await until(() => document.querySelector('.magic-tool .candidate'), 20000);\n      await sleep(600);\n      return (!!input && input.value === '3' && !document.querySelector('.depth-status') && !!candidate) || { profondeur: input?.value, statut: document.querySelector('.depth-status')?.textContent };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-depuis-it-tools",
    "url": "http://localhost:5173/tools/base64-string-converter",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      const link = await until(() => document.querySelector('.recipes-link'));\n      if (!link) return 'lien vers les Recettes absent';\n      link.click();\n      await until(() => location.pathname === '/tools/recipes' && stepNames().length);\n      return stepNames().join(' + ') === 'To Base64 + From Base64' || { étapes: stepNames() };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-depuis-operation",
    "url": "http://localhost:5173/tools/to-hex",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await until(() => document.querySelector('.cc-op .io textarea'));\n      type(document.querySelector('.cc-op .io textarea'), 'abc');\n      await sleep(300);\n      [...document.querySelectorAll('.op-toolbar button')][0].click();\n      await until(() => location.pathname === '/tools/recipes' && stepNames().length);\n      const out = await output();\n      return (stepNames().join() === 'To Hex' && out === '61 62 63') || { étapes: stepNames(), sortie: out };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-partage",
    "url": "http://localhost:5173/tools/recipes",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      (await until(() => document.querySelector('.workbench .empty .resume')))?.click();\n      await until(() => stepNames().length);\n      [...document.querySelectorAll('.toolbar-actions button')].find(b => /Partager|Share/.test(b.textContent)).click();\n      const link = await until(() => document.querySelector('.share textarea')?.value);\n      const cyberchef = document.querySelector('.share-actions a')?.getAttribute('href') ?? '';\n      return (link.includes('/tools/recipes#recipe=To_Hex') && cyberchef.startsWith('https://gchq.github.io/CyberChef/#recipe=To_Hex')) || { lien: link, cyberchef };\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "recettes-lien-interne",
    "url": "http://localhost:5173/tools/recipes#recipe=Magic(3,false,false,'')&input=YUdWc2JHOD0",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      const link = await until(() => document.querySelector('.html-output a[href^=\"#recipe=\"]'), 20000);\n      if (!link) return 'aucun lien de piste dans la sortie de Magic';\n      link.click();\n      await until(() => stepNames().join() === 'From Base64');\n      const out = await output();\n      return (stepNames().join() === 'From Base64' && out === 'hello') || { étapes: stepNames(), sortie: out };\n    })()"
  },
  {
    "scheme": "dark",
    "wait": 300,
    "name": "recettes-mobile",
    "url": "http://localhost:5173/tools/recipes#recipe=From_Base64('A-Za-z0-9%2B/%3D',true,false)To_Hex('Space',0)&input=YUdWc2JHOD0",
    "width": 390,
    "height": 844,
    "mobile": true,
    "fullPage": true,
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  const until = async (test, timeout = 15000) => {\n    for (const started = performance.now(); performance.now() - started < timeout;) {\n      const value = test();\n      if (value) return value;\n      await sleep(100);\n    }\n    return null;\n  };\n  const type = (element, value) => {\n    element.value = value;\n    element.dispatchEvent(new Event('input', { bubbles: true }));\n  };\n  const addOperation = async (query) => {\n    const field = document.querySelector('.picker .field-input');\n    field.focus();\n    type(field, query);\n    await sleep(250);\n    field.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', bubbles: true }));\n    await sleep(250);\n  };\n  const stepNames = () => [...document.querySelectorAll('.steps .step-name')].map(e => e.textContent.trim());\n  const idle = () => until(() => !document.querySelector('.workbench [aria-busy=\"true\"], .magic-tool [aria-busy=\"true\"]'), 20000);\n  const output = async () => {\n    await sleep(500);\n    await idle();\n    return document.querySelectorAll('.io-column textarea')[1]?.value ?? null;\n  };\n  const inputField = () => document.querySelector('.io-column textarea');\n  await until(() => document.querySelector('.workbench, .magic-tool, .tool-content'));\n\n      await until(() => stepNames().length === 2);\n      return (await output()) === '68 65 6c 6c 6f';\n    })()"
  },
  {
    "scheme": "light",
    "wait": 300,
    "name": "accueil-a-la-une",
    "url": "http://localhost:5173/",
    "script": "(() => [...document.querySelectorAll('.tiles .tile')].map(a => a.getAttribute('href')).join() === '/tools/recipes,/tools/magic,/tools/stego-lab')()"
  }
]
