[
  {
    "name": "tactile-tel-accueil",
    "url": "http://localhost:5173/",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-categorie",
    "url": "http://localhost:5173/categories/crypto",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-outil",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-operation",
    "url": "http://localhost:5173/tools/to-hex",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-atelier",
    "url": "http://localhost:5173/tools/recipes#recipe=From_Base64('A-Za-z0-9%2B/%3D',true,false)Gunzip()",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-magic",
    "url": "http://localhost:5173/tools/magic",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-stego",
    "url": "http://localhost:5173/tools/stego-lab",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-parametres",
    "url": "http://localhost:5173/settings",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-a-propos",
    "url": "http://localhost:5173/about",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-palette",
    "url": "http://localhost:5173/",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  document.dispatchEvent(new KeyboardEvent('keydown', { key: 'k', ctrlKey: true, bubbles: true })); await sleep(500);\n  const field = document.querySelector('.palette input'); field.value = 'hex'; field.dispatchEvent(new Event('input', { bubbles: true })); await sleep(400);\n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tel-tiroir",
    "url": "http://localhost:5173/",
    "width": 390,
    "height": 844,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  document.querySelector('.top-bar-menu').click(); await sleep(500);\n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-accueil",
    "url": "http://localhost:5173/",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-categorie",
    "url": "http://localhost:5173/categories/crypto",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-outil",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-operation",
    "url": "http://localhost:5173/tools/to-hex",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-atelier",
    "url": "http://localhost:5173/tools/recipes#recipe=From_Base64('A-Za-z0-9%2B/%3D',true,false)Gunzip()",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-magic",
    "url": "http://localhost:5173/tools/magic",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-stego",
    "url": "http://localhost:5173/tools/stego-lab",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-parametres",
    "url": "http://localhost:5173/settings",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-a-propos",
    "url": "http://localhost:5173/about",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-palette",
    "url": "http://localhost:5173/",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  document.dispatchEvent(new KeyboardEvent('keydown', { key: 'k', ctrlKey: true, bubbles: true })); await sleep(500);\n  const field = document.querySelector('.palette input'); field.value = 'hex'; field.dispatchEvent(new Event('input', { bubbles: true })); await sleep(400);\n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-tab-tiroir",
    "url": "http://localhost:5173/",
    "width": 900,
    "height": 1200,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  document.querySelector('.top-bar-menu').click(); await sleep(500);\n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-accueil",
    "url": "http://localhost:5173/",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-categorie",
    "url": "http://localhost:5173/categories/crypto",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-outil",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-operation",
    "url": "http://localhost:5173/tools/to-hex",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-atelier",
    "url": "http://localhost:5173/tools/recipes#recipe=From_Base64('A-Za-z0-9%2B/%3D',true,false)Gunzip()",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-magic",
    "url": "http://localhost:5173/tools/magic",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-stego",
    "url": "http://localhost:5173/tools/stego-lab",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-parametres",
    "url": "http://localhost:5173/settings",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-a-propos",
    "url": "http://localhost:5173/about",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  \n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-palette",
    "url": "http://localhost:5173/",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "(async () => {\n  const sleep = ms => new Promise(r => setTimeout(r, ms));\n  await sleep(1200);\n  document.dispatchEvent(new KeyboardEvent('keydown', { key: 'k', ctrlKey: true, bubbles: true })); await sleep(500);\n  const field = document.querySelector('.palette input'); field.value = 'hex'; field.dispatchEvent(new Event('input', { bubbles: true })); await sleep(400);\n  const SELECTOR = 'a[href], button, [role=tab], [role=option], [role=separator][tabindex], [role=switch], summary, input:not([type=hidden]), select';\n  // Nos composants à l'intérieur de .tool-content ; le reste y appartient aux outils.\n  const OURS = '.workbench-actions, .cc-input .io-head, .cc-output .io-head, .cc-input .io-strip, .ops, .step-head, .pane-head, .drop';\n  const foreign = el => Boolean(el.closest('.tool-content')) && !el.closest(OURS);\n  const describe = el => {\n    const cls = typeof el.className === 'string' ? el.className.split(' ').filter(Boolean).slice(0, 2).join('.') : '';\n    const label = (el.getAttribute('aria-label') || el.textContent || el.getAttribute('placeholder') || '').trim().replace(/\\s+/g, ' ').slice(0, 24);\n    return el.tagName.toLowerCase() + (cls ? '.' + cls : '') + ' «' + label + '»';\n  };\n  // Palette ouverte : seule elle reçoit le doigt, la page derrière est couverte.\n  const modal = document.querySelector('.palette');\n  const small = [];\n  for (const el of document.querySelectorAll(SELECTOR)) {\n    if (el.closest('[inert]') || foreign(el) || (modal && !modal.contains(el))) continue;\n    // Lien de ligne : il s'étend sous toute la ligne, c'est elle la cible.\n    const target = el.classList.contains('tool-row-link') ? el.closest('.tool-row') : el;\n    const r = target.getBoundingClientRect();\n    if (r.width < 1 || r.height < 1) continue;\n    const style = getComputedStyle(el);\n    if (style.visibility === 'hidden' || style.display === 'none' || Number(style.opacity) === 0) continue;\n    if (el.tagName === 'A' && el.closest('p') && style.display === 'inline') continue;\n    const iconOnly = !el.textContent.trim();\n    if (r.height < 43.5 || (iconOnly && r.width < 43.5)) small.push(describe(el) + ' ' + Math.round(r.width) + 'x' + Math.round(r.height));\n  }\n  return small.length ? small : true;\n})()"
  },
  {
    "name": "tactile-grand-marque",
    "url": "http://localhost:5173/tools/hash-text",
    "width": 1366,
    "height": 1024,
    "mobile": true,
    "scheme": "dark",
    "script": "getComputedStyle(document.querySelector('.top-bar-brand')).display === 'none'"
  }
]
